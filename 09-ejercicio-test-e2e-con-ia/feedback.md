Hola! Muy bien hecho!
Me gustó como planteaste los tests, hicimos algunas modificaciones para mejorar el nivel de accesibilidad y la robustez.

Dejamos comentarios en cada parte, la idea es siempre usar selectores como `getByRole` o `getByLabel` y no usar selectores débiles como `locator` o por CSS.

Cualquier duda nos avisas si?
Un saludo y a seguir trabajando!s
